{
  "name": "rrt-tecidos",
  "version": "0.0.0",
  "private": true,
  "description": "RRT — auditoria da revisão de rolos de tecido (Google Apps Script)",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "globais": "node tests/ferramentas/globais.js",
    "gerar": "node tools/gerar.js"
  },
  "engines": {
    "node": ">=22"
  }
}
